spring:
  config:
    activate:
      on-profile: prod

  datasource:
    url: ${DB_URL}
    username: ${DB_USERNAME}
    password: ${DB_PASSWORD}

logging:
  level:
    org.hibernate.SQL: warn

jwt:
  secret: ${JWT_SECRET}

server:
  address: 0.0.0.0
  port: 8080
